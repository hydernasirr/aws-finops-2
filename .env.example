# AWS Credentials (Optional - can also configure via dashboard)
# If not provided, will use default AWS credential chain (IAM roles, ~/.aws/credentials, etc.)

AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here
AWS_REGION=us-east-1

# Note: The IAM user/role needs the following permissions:
# - ce:GetCostAndUsage
# - ce:GetCostForecast
# - ec2:DescribeInstances
# - ec2:DescribeVolumes
# - ec2:DescribeAddresses
# - rds:DescribeDBInstances
